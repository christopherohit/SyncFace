torch-ema
ninja
trimesh
opencv-python
tensorboardX
numpy==1.24.4
pandas==2.0.3
tqdm
matplotlib
PyMCubes==0.1.4
rich
dearpygui
packaging
scipy
scikit-learn
transformers==4.36.0
face_alignment==1.4.1
python_speech_features
numba
resampy
pyaudio
soundfile
einops
configargparse
mediapipe
lpips
imageio-ffmpeg
onnxruntime-gpu
librosa
fvcore
iopath

# Enhanced Audio Encoder Dependencies
# Foundation models for improved prosodic understanding
accelerate>=0.20.0  # For efficient model loading
torchcrepe>=0.0.15  # For pitch extraction (optional)
praat-parselmouth>=0.4.3  # For prosody analysis (optional)

# Note: The following models are supported via transformers (already installed):
# - Whisper (openai/whisper-*): Rich prosodic and semantic understanding
# - SpeechT5 (microsoft/speecht5_*): Speech representation learning
# - EnCodec (facebook/encodec_*): High-quality audio compression features
# No additional packages needed if transformers>=4.36.0 is installed

# Improved Lip-Sync Dependencies
# AV-HuBERT and Wav2Vec2 for phoneme-rich features
fairseq>=0.12.2  # For AV-HuBERT (optional, only if using avhubert model)
# Wav2Vec2 is supported via transformers (already installed)

# Multi-Scale Sync Loss Dependencies
# Uses frozen SyncNet from Wav2Lip for contrastive audio-video alignment
# No additional packages needed - implemented using PyTorch
